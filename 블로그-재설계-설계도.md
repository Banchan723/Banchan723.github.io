# 블로그 재설계 — 설계도 (어떻게 구현할지)

> **상태: 설계 확정 (2026-06-09).** 기획 방향(`블로그-재설계-기획-DRAFT.md`)은 GO 완료.
> 이 문서 = 그 방향을 *어떻게 구현*할지의 설계도. Codex(GPT-5.5) 교차검증 1회 거침.
> 정본(SYSTEM-SPEC.md)은 아직 안 고침 — **이 설계도대로 구현 → 검증 → SYSTEM-SPEC 개정**이 순서.
> 사용자 결정 반영: 의미검증=비차단 2단계(+맞춤법·오타), 기존 6글=전부 재작성.

---

## 0. 한 장 요약 (이 설계의 핵심)

블로그 글이 얕은 진짜 원인은 글쓰기가 아니라 **공부가 얕아서**다. 그래서 손대는 곳은 글 작성기가 아니라 **학습 게이트**다. 추가로 Codex 교차검증이 준 가장 중요한 교정:

> **"검증"을 한 덩어리로 보지 말고 3종으로 쪼갠다. 발행을 막는 건 LLM이 아니라 '증거 검사'여야 공짜 파이프가 안 깨진다.**

| 검증 종류 | 무엇을 보나 | 누가 | 발행 차단? |
|---|---|---|---|
| **① 기계 검증** | 코드↔출력 짝이 있나, 증거가 비었나, 형식·스키마 맞나 | CI (LLM 없음, 공짜) | **차단 (하드 게이트)** |
| **② 학습 검증** | 토픽을 변형 상황에서도 다루나 (암기 아님) | Claude 앱 채점 게이트 | 통과해야 글 작성 |
| **③ 의미 검증** | 설명이 사실로 맞나 + 맞춤법·오타 | Claude 자가검토 + (집)Codex | **비차단 (보조)** |

핵심 = ①을 강하게, ②를 깊게, ③은 얹되 막지 않는다. 이러면 "태블릿만으로 공짜 자동발행" 원칙이 유지된다.

---

## 1. 확정된 결정 (3대 + 사용자 선택)

**결정1 — 의미 검증 위치: 비차단 2단계 (사용자 선택 1번 + 맞춤법/오타 추가)**
- 1단계(상시·무료·태블릿): 노션 글 작성 후 Claude가 **검사관 모드**로 자가 적대적 재검토 — 사실오류 + 맞춤법·오타. 노션 상태에 `자가검토완료` 한 칸 추가.
- 2단계(가끔·집): 집 Claude Code 켤 때 Codex로 발행된/대기 글 **일괄 리뷰** ("리뷰 부채 청산"). 진짜 다른 모델 눈.
- **발행은 안 막는다.** 의미검증은 차단 게이트가 아님 → 공짜 파이프·태블릿 원칙 유지. 사실오류는 드물게 잠깐 라이브 후 수정.

**결정2 — 학습 게이트: '느낌'이 아니라 '행동'으로 강화**
- "토픽 전체를 깊이 이해" 같은 추상 기준은 관대편향에 무너진다. 통과 조건을 **변형 가능성**으로 못 박는다 (3절).
- 토픽 경계는 roadmap.yaml 노드 + 각 노드에 **scope(include/exclude)** 추가로 "비대해짐/빠뜨림" 동시 방어 (2절).

**결정3 — 글 구조: 겉은 교육 레퍼런스, 속은 공부일지 (반슬롭 장치는 메타데이터로 유지)**
- 보이는 골격은 6개 교육 레퍼런스 구조. 단 기존 8섹션의 반슬롭 장치(막힘·증거·미지 고백)를 **각 주장에 증거 붙이기 + 메타 블록**으로 녹여 잃지 않는다 (4절).

**사용자 결정 — 기존 6글: 전부 재작성** (6절). 단 진짜 깊어지려면 각 토픽을 다시 깊게 공부해야 함(형식만 바꾸면 의미 없음).

---

## 2. roadmap.yaml 노드 확장 (토픽 경계 = 학습 단위 = 검증 단위)

토픽 경계를 "비대해지지도, 빠뜨리지도" 않게 하려면 노드에 **scope**와 **필수 증거 목록**을 박는다. 이게 학습·글·검증 셋의 공통 기준이 된다.

기존 노드:
```yaml
- { title: "참조", status: published, slug: "cpp-reference" }
```

확장 노드(제안):
```yaml
- title: "참조"
  status: published
  slug: "cpp-reference"
  topic_id: "cpp.reference.basic"      # 안정 식별자 (글 front matter와 연결)
  scope:
    include: [참조 선언, 별칭 의미, 참조로 전달, const 참조 기초]
    exclude: [rvalue 참조, move 의미론, 완벽 전달]   # 별도 노드에서 다룸
  required_evidence:                    # CI가 '있나 없나'만 본다 (사실판단 X)
    - 직접_돌린_출력
    - 컴파일_실패_예제                   # 있으면 강력 (없어도 됨 = 경고)
```

- **URL·기존 필드 불변.** title/status/slug 그대로 두고 *아래에 추가*만. 기존 6개 노드는 점진 확장.
- scope가 없으면 한 글이 계속 비대해지거나(참조에 move까지 끌고 옴) 빠진 걸 모른 채 지나감 → Codex가 짚은 핵심 리스크.
- `/roadmap/` 페이지 렌더는 기존대로(추가 필드는 안 보여도 됨). 나중에 노드 클릭 시 scope를 "이 글에서 다루는 것/안 다루는 것"으로 표시 가능.

---

## 3. 학습 게이트 강화 — 통과 조건을 '행동'으로 (SYSTEM-SPEC 4-2 개정안)

현 4-2는 "자기 말 설명 + 변형 1~2 + 직접 출력 + 착각 정정"이다. 방향은 맞으나 "토픽 전체 깊이"로 올리려면 **통과 행동을 명시**해야 한다. Codex 권고 채택:

### 노드 통과 조건 (모두 만족해야 '발행준비')
한 토픽(roadmap 노드)을 통과하려면 — 그 노드 scope 안에서:
1. **자기 말 정의** — 개념을 내 언어로 (교과서·AI 복붙 아님).
2. **왜 필요한가** — 기존 방식의 문제와 연결해 설명.
3. **변형 3개** — 본 예제와 다른 입력/조건으로 직접 써본다 (암기 배제).
4. **컴파일 됨/안 됨 구분** — 되는 예제 + 안 되는 예제(에러 메시지 일부)를 구분.
5. **흔한 오해 반례** — 오해 1개 이상을 틀린 코드 + 왜 틀렸는지로.
6. **출력 예측·해석** — 직접 돌린 출력 + 왜 그렇게 나오는지.
7. **경계 인식** — scope의 include는 다뤘고 exclude는 "이 글 밖"이라 분리.

> 핵심은 "이해했다"가 아니라 **변형 상황에서 안 무너지나.** 정의 암기는 변형에서 깨진다.

### 관대편향 약화 장치 (같은 Claude 채점 보정)
- 채점 전 **채점 기준표 먼저** 만들고 그다음 답안 본다(답 보고 기준 맞추기 방지).
- "정답?"만 묻지 말고 **반례 생성**을 시킨다.
- 답안 뒤 "**내 답의 취약점 3개**"를 따로 뽑게 한다.
- **틀린 코드 판별 문제**(일부러 버그 섞기) 포함.
- 설명형보다 **출력 예측·컴파일 판단·버그 수정** 비중을 늘린다.

→ 통과 시 노션 `통과근거`에 한 줄(기존 유지). 자기 블로그라 강제력은 결국 이 흔적 + 자기규율.

---

## 4. 새 글 구조 (교육 레퍼런스 6골격 + 증거 메타)

### 보이는 골격 (독자가 읽는 순서)
1. **개념 정의** — 한 문장 정의 + **이 글의 범위 / 안 다루는 범위**(노드 scope에서).
2. **왜 필요한가** — 기존 방식의 문제 → 이 개념이 푸는 것 → 언제 쓰나.
3. **문법과 동작** — 내가 공부한 만큼 빠짐없이. 컴파일러가 허용하는 형태, 값/타입/메모리 관점.
4. **직접 확인한 예제** — 실행 코드 + 실행 명령 + **실제 출력** + 왜 그 출력인지. (컴파일 실패 예제 있으면 에러 일부)
5. **흔한 실수와 오해** — 오해 → 틀린 코드 → 왜 틀렸는지 → 고친 코드.
6. **관련 토픽과 다음 경계** — 다룬 것 / **아직 모르는 것** / 다음 토픽 링크(계층 네비 자동).

### 반슬롭 장치는 어디 갔나 (Codex의 핵심 경고)
6골격으로 바꾸면 8섹션의 정직 장치(막힘·증거·미지 고백)가 약해진다. 그래서 **큰 섹션이 아니라 규칙·메타로 강제**한다:

> **규칙: 글 안의 중요한 기술 주장마다 증거 1개를 붙인다.** 증거는 다음 중 하나 — ① 직접 돌린 코드 ② 직접 돌린 출력 ③ 컴파일 실패 로그 ④ 공식 문서/표준 링크 ⑤ 이전 글에서 검증한 개념 링크.

글 끝(또는 front matter)에 **검증 메타 블록**을 둔다:
```yaml
verification:
  checked_claims:
    - { claim: "참조는 반드시 초기화돼야 한다", evidence: "본문 예제2 컴파일에러" }
    - { claim: "참조로 전달하면 원본이 바뀐다", evidence: "본문 예제1 출력" }
unknowns:
  - "rvalue 참조는 이 글에서 안 다룸"
```
- `unknowns` = 옛 "아직 모르는 것"의 정직 장치를 메타로 보존(6골격에도 들어가지만 기계검증용으로 한 번 더).
- "막혔던 점"은 **선택 유지** — 있으면 6번 또는 본문에 자연스럽게. 없으면 지어내지 말 것(가짜 서사 금지, 기존 정본 유지).

---

## 5. validate_posts.py 재설계 (기계 검증 = 하드 게이트)

현재 검증기는 8섹션 강제. 새 구조로 바꾸되 **증거 강제는 더 세게**.

### 바뀌는 점
- **필수 섹션 교체**: 옛 6개(이 글에서 이해할것/읽기전 최소배경/확인한증거/그래서 이렇게/아직모르는것/확인질문) → 새 6골격 키워드. "읽기 전 최소 배경"은 곁가지라 **제거**.
  - 새 필수 키워드(부분일치): `개념`/`정의`, `왜`/`필요`, `문법`/`동작`, `예제`/`확인한`, `실수`/`오해`, `관련`/`다음`.
- **검증 메타 블록 강제(신규)**: front matter나 본문 끝 `verification:` + `unknowns:` 존재 검사. 없으면 FAIL.
- **매체별 증거 강제 유지·강화**(기존 로직 재사용):
  - code: 코드펜스 + `## 직접 확인한 예제`에 실제 출력(placeholder 불가) — 기존 `_is_real_output` 로직 그대로.
  - visual: 이미지 + 재현 단계.
  - video: 출처 URL + 재현/변형 결과.
- **(미래 옵션, 지금 안 함)**: CI가 실제 C++ 컴파일·출력 대조. Codex가 권한 강수지만 **태블릿 워크플로(노션 텍스트로 글 씀, 예제 파일 따로 커밋 안 함)와 마찰** → 보류. 글 내 코드↔출력 짝 검사로 갈음. 나중에 `examples/` 디렉터리 관례가 생기면 도입.
- **변경 없음**: front matter 필수필드, taxonomy, slug/canonical 중복, title 따옴표 — 다 유지.

### 재사용하는 기존 자산 (안 버림)
`section_index`, `section_with_children`, `parse_fences`, `_is_real_output`, `prose_paragraphs` 등 파싱 함수는 견고하니 그대로. **REQUIRED_SECTIONS 상수와 메타블록 검사만 교체.**

---

## 6. 의미 검증 워크플로 (비차단 2단계 + 맞춤법·오타)

### 1단계 — Claude 자가 적대적 재검토 (상시, 태블릿, 무료)
글 작성 직후, **역할을 검사관으로 바꿔** 다음을 본다:
- 사실오류·틀린 이해 (scope 안에서, 범위 넘은 일반론 없나).
- **맞춤법·오타** (사용자 요청 반영).
- 각 주장에 증거가 붙었나(4절 규칙).
- 발견 시 노션에 수정. → 통과하면 상태 `자가검토완료` → `발행준비`.

### 2단계 — Codex 일괄 리뷰 (가끔, 집)
집 Claude Code 켤 때, 발행됐거나 대기 중인 글을 Codex로 일괄 검사("진짜 다른 모델 눈"). 사실오류 발견 시 노션/글 수정. 강제 아님 — 리뷰 부채를 주기적으로 청산.

### 노션 상태머신 변경
```
공부중 → (채점 통과 + 본문 작성) → 작성완료
       → (Claude 자가검토: 사실·맞춤법·증거) → 자가검토완료 = 발행준비
       → (CI 기계검증 통과) → 발행완료
            ↑ Codex 일괄 리뷰는 발행 후에도 비동기로 (부채 청산)
```
- 기존 상태에 `작성완료`·`자가검토완료` 한두 칸 추가. (또는 `발행준비`를 "자가검토까지 끝난 상태"로 의미 강화 — 칸 안 늘리는 쪽이 단순. **택일은 구현 시 사용자 확인.**)

---

## 7. 계층 네비게이션 (글 ↔ 트리 연결)

roadmap.yaml을 단일 소스로:
- **상단 breadcrumb**: `C++ > 메모리와 포인터 > 참조` — 글 front matter의 `topic_id`로 트리에서 위치 역추적.
- **하단 자동 링크**: 같은 그룹 형제 노드, 다음 planned 노드 → "다음에 배울 것".
- 구현: roadmap.yaml → 빌드 시 인덱스(topic_id→경로) 생성 → Hugo partial이 글의 topic_id로 렌더. **새 partial 1개 + front matter에 topic_id 1줄.** URL·파일구조 불변.
- front matter에 `topic_id` 추가 필요 → validate에 선택 필드로(있으면 형식만 검사).

---

## 8. 기존 6글 재작성 계획 (사용자: 전부 재작성)

대상: 포인터·참조·new/delete·스마트포인터·클래스·const (slug 전부 불변).

- **slug·URL·canonical_topic 절대 불변** (라이브 글 `/p/cpp-pointer/` 외부 공개됨).
- 재작성 = 같은 파일 `content/post/<slug>/index.md` 내용 교체. front matter 핵심키 유지.
- **순서**: 포인터 1개를 새 구조로 먼저(시범) → 새 검증기 통과 확인 → 나머지 5개.
- ⚠ **형식만 바꾸면 의미 없음.** 각 토픽을 노드 scope 기준으로 *다시 깊게 공부*해야 진짜 깊어진다(이게 이 재설계의 전부). 재작성은 "새 구조로 옮기기"가 아니라 "더 깊게 공부한 결과를 새 구조에 담기".
- 6글 재작성은 새 학습 게이트(3절)·새 구조(4절)·새 검증기(5절)의 **실전 검증**도 겸한다.

---

## 9. 구현 순서 (마일스톤)

1. **roadmap.yaml 노드 확장** (scope/required_evidence/topic_id) — 6개 발행 노드부터. (2절)
2. **validate_posts.py 재설계** — 새 섹션·검증 메타 블록·증거 강제. 기존 파싱함수 재사용. (5절)
3. **글 1개 시범 재작성**(포인터) — 새 구조로, 새 검증기 통과까지. (8절)
4. **계층 네비 partial** — breadcrumb + 다음 토픽 링크. (7절)
5. **나머지 5글 재작성.** (8절)
6. **SYSTEM-SPEC.md 정본 개정** — 4-1(구조)·4-2(채점)·5(규칙)·6-2(노션 템플릿)·7(상태머신) 반영. (아래 10절)
7. **노션 "블로그 발행 규칙" 페이지 갱신** — Claude 앱이 읽는 지침을 새 게이트·새 구조·자가검토 단계로.

> 1·2·4는 글과 독립이라 먼저/병렬 가능. 3이 가장 중요한 검증점(새 시스템이 진짜 깊은 글을 내나).

---

## 10. SYSTEM-SPEC.md 개정 대상 (구현 확정 후)

정본은 **구현·검증 후** 고친다(문서-구현 드리프트 방지, 사용자 최종 승인 후):
- **4-1 (3) 본문 골격**: 8섹션 → 6골격 + 증거 규칙 + 검증 메타 블록.
- **4-2 통과 기준**: 행동 조건 7개 + 관대편향 약화 장치.
- **5 절대 규칙**: 의미검증 비차단 단계, 검증 메타 블록 필수 추가.
- **6-2 노션 템플릿**: `## 블로그 본문` 구조를 6골격으로, `자가검토완료` 상태.
- **7 상태머신**: `작성완료`·`자가검토완료` 반영.
- **3 한계**: "교차검증 3종 분리, 의미검증은 비차단" 명시.

⚠ 구체 수치(변형 3개 등)는 정본에 의도만, 세부는 검증기/규칙 페이지에서 관리(글로벌 규칙: 기획 정본에 수치 박지 않기).

---

## 11. 알고 가는 트레이드오프 (수용)

- **의미검증 비차단** → 사실오류가 드물게 잠깐 라이브될 수 있음. 수용(공짜·태블릿 원칙 우선, 다음 세션 수정).
- **같은 Claude 채점 관대편향** → 약화 장치로 줄이나 완전 제거 불가. 행동 조건 + 통과근거 흔적이 방어선.
- **CI 실제 컴파일 안 함** → 글에 적힌 출력이 진짜 그 코드 출력인지 기계가 100% 보증 못 함. "직접 돌린 것만" 자기규율 + Codex 2단계 리뷰로 보완.
- **재작성 노동** → 6글 다시 깊게 공부 = 큰 일. 하지만 이게 재설계의 본질(공부를 깊게).
