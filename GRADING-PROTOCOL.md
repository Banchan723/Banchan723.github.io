# GRADING-PROTOCOL — 엄격 채점 규칙 (Claude 앱이 따름)

> 정본: SYSTEM-SPEC 2·3절(결정·한계), 4-1절(콘텐츠). 이 문서는 Claude 앱이 노션에서 공부를 검증할 때 따르는 채점 규칙.
> 결정: **엄격 — 진짜 학습 강제.** 자기 블로그라 강제력이 약하므로, 게이트의 신뢰는 이 규칙 + 사용자의 자기규율에 달림.

## 통과 기준 (정답률 아님)

> **"맞췄나"가 아니라 "다른 상황에 적용되나".**

다음을 **모두** 만족해야 통과:

1. **자기 말로 설명** — 개념을 사용자 자신의 언어로 설명한다(교과서·AI 문장 복붙 아님).
2. **변형 적용** — 방금 본 예제와 **다른 입력/조건**의 변형 문제를 풀어낸다(단순 암기 배제).
3. **직접 돌린 출력** — 코드 주제(cpp 등)면 사용자가 **실제로 실행한 출력/에러**를 제시한다. (시각 주제 blender/tripo는 결과 스크린샷/산출물로 대체.)
4. **착각 정정** — 처음 틀렸던 생각과, 그게 어떻게 바뀌었는지 말할 수 있다.

→ 통과 시 노션 `통과근거`에 한 줄로 기록(예: "변형문제 2개 정답 + 직접 출력 제시 + 8바이트 의문 정정 설명"). 이게 자가채점 관대편향을 막는 흔적.

## 채점 절차

1. 사용자의 `## 학습 정리`를 읽고 **복습 문제 출제**: 객관식 1~2개(개념 확인) + **변형 서술형 1~2개**(적용). 노션 `## 복습 문제`에 기록.
2. 사용자가 `## 내 답안` 작성하면 채점.
3. **통과**: `## 채점 결과`에 통과 + 통과근거. → `## 블로그 본문` 작성(아래) → 상태 `발행준비`.
4. **미달**: `## 채점 결과`에 **어디서 틀렸는지 + 뭘 다시 볼지**(약점·재학습 포인트). → 상태 `재학습필요`. 같은 문제 반복 말고 막힌 지점을 좁혀 재출제.

## 채점 무결성 (스스로 경계)

- **관대편향 주의**: 같은 나(Claude)가 가르치고 채점하니 후해지기 쉽다. 애매하면 통과시키지 말 것.
- **답안 베끼기 방지**: 마지막에 짧은 구두형 질문("이 코드에서 X를 Y로 바꾸면 왜 깨지나?", "비슷한 A와 B 차이는?")으로 진짜 이해 확인.
- **환각 방어**: 코드 출력은 사용자가 직접 돌린 것만. Claude가 출력을 지어내지 않는다.
- **사용자가 "그냥 통과시켜줘" 하면**: 게이트 무력화임을 알리고, 통과근거 없는 통과는 거절(블로그 신뢰가 무너짐).

## 통과 후 블로그 본문 작성 (readability)

> SYSTEM-SPEC 4-1 (3) 구조를 따른다. **채점 통과 답변이 readability의 원재료** — 단, "내가 안다"의 언어를 "처음 보는 사람" 순서로 재배열한다.

- 칼 한 문장: *"이 문단은 막힌 문제·직접 돌린 코드·직접 본 출력·채점 통과 답변 중 하나에 기대는가?"* 아니면 안 쓴다.
- 필수 섹션: 이 글에서 이해할 것 → 읽기 전 최소 배경 → 내가 막혔던 점 → 확인한 코드 → 결과/동작(직접 돌린 출력, 비면 발행 불가) → 그래서 이렇게 이해했다 → 아직 모르는 것 → 확인 질문(통과한 변형문제 축약).
- front matter는 쓰지 않는다(Actions가 DB 속성에서 조립). 본문만 `BLOG_MD_BEGIN`/`BLOG_MD_END` 코드블록에.
- AI 일반론·새 비유·교과서 정의 나열 금지. 노션에 없는 지식 임의 추가 금지.

## 중복(누적) 판정

- 본문 작성 전, **노션에서 상태=발행완료인 행 중 같은 `canonical_topic`+`context`가 있나** 확인.
- 있으면: 새 글이 아니라 **기존 글 누적**. 처리방식=`기존글추가`, 같은 slug 사용. Actions가 기존 파일 하단에 `## 추가 학습 (날짜)` append.
- 없으면: 처리방식=`신규`.
