# NOTION-DB — 학습 기록 노션 설정서

> 정본: SYSTEM-SPEC 6절. 노션은 "공부한 장소(모바일/데스크탑)"와 "발행 장소(데스크탑)"를 잇는 다리다.
> 어디서 공부했든 여기에 들어가면, 집에서 Cowork가 블로그로 발행한다.

## 1. 노션 데이터베이스(표) 만들기

노션에서 **데이터베이스 - 표(Table)** 하나를 만든다. 이름 예: `학습 기록`.
각 **행 = 학습 주제 1개**. 칼럼은 아래대로.

| 칼럼 이름 | 타입 | 값 예시 | 용도 |
|---|---|---|---|
| 제목 | 제목(Title) | `C++ 포인터` | 글 제목 후보 (한글 OK) |
| canonical_topic | 텍스트 | `pointer` | **중복 판정 키①** (영문 소문자) |
| context | 선택(Select) | `cpp` / `unreal` / `blender` / `tripo` | **중복 판정 키②** |
| 난이도 | 선택 | `입문` / `기초` / `중급` | level 매핑 |
| 블로그行 | 체크박스 | ☑ / ☐ | 발행 대상 여부 |
| 상태 | 선택 | `미정리` / `정리완료` / `초안` / `검토중` / `발행완료` | 진행 추적 |
| 발행일 | 날짜 | `2026-06-06` | 발행 후 자동 기록 |

> **중복 판정 키 = canonical_topic + context.** 같은 키면 같은 글(수정/추가 대상), 다른 키면 새 글.
> 예: `pointer`+`cpp` 와 `pointer`+`unreal` 은 **다른 글**이다.

## 2. 각 행의 본문 — 고정 템플릿 (자유 메모 금지)

행을 열면 본문에 **항상 이 6칸**을 채운다. 형식이 고정돼야 AI가 일관된 글을 뽑는다.

```
## 오늘 배운 것
(핵심을 내 말로 한두 줄)

## 막혔던 부분 · 헷갈린 것
(여기가 제일 중요. 블로그의 "내가 헷갈렸던 점" 섹션 재료가 된다)

## 해결한 방법 · 비유
(어떻게 이해됐는지, 기억나는 비유)

## 중요 개념
(키워드·정의)

## 코드 예시
(직접 쳐본 거. 없으면 비워둠)

## 아직 헷갈리는 것
(다음에 더 팔 거리)
```

**왜 "막혔던 부분"이 핵심인가** — AI는 매끈한 설명은 잘 쓰지만, *네가 어디서 헷갈렸는지*는 못 지어낸다. 그게 네 글을 "네 글"로 만들고, 같은 데서 막히는 다른 초보에게 가장 큰 도움이 된다. **절대 비우지 말 것.**

## 3. 난이도 매핑

| 노션 | front matter level |
|---|---|
| 입문 / 기초 | beginner |
| 중급 | intermediate |
| 고급 | advanced |

## 4. 흐름

1. 공부하다 한 주제가 끝나면 → 노션에 새 행 추가, 6칸 채움.
2. 다 채웠으면 → `상태 = 정리완료`, 블로그에 올릴 거면 `블로그行 ☑`.
3. 집 데스크탑에서 Cowork에게 발행 요청 (COWORK-PUBLISH.md 참고).
4. 발행되면 Cowork가 `상태 = 발행완료`, `발행일` 자동 기록.

> 밖에서는(모바일) 1~2단계까지만. 발행(3단계)은 집 데스크탑에서.
