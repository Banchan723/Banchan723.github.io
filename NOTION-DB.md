# NOTION-DB — 학습 기록 노션 설정서 (v2: 검증 게이트)

> 정본: SYSTEM-SPEC. 노션은 "공부한 장소(어디서나)"와 "발행 장소(GitHub)"를 잇는 다리 + 학습 검증의 무대.
> v2(2026-06-06): 검증 게이트 추가 — 공부→출제→답안→채점→통과해야 발행.
> ⚠️ 이 DB는 **아직 안 만들어짐.** 다음 세션에서 Notion MCP로 생성 예정(NEXT-SESSION 참고).

## 1. 데이터베이스 만들기

노션에서 **데이터베이스 - 표(Table)** 하나. 이름: `학습 기록`. 각 **행 = 학습 주제 1개**.

### 속성(칼럼)

| 칼럼 | 타입 | 값 예시 | 용도 |
|---|---|---|---|
| 제목 | 제목(Title) | `C++ 포인터` | 글 제목 (한글 OK, Actions가 따옴표 처리) |
| canonical_topic | 텍스트 | `pointer` | **중복키①** (영문 소문자) |
| context | 선택 | `cpp`/`unreal`/`blender`/`tripo` | **중복키②** + 카테고리 매핑 |
| 태그 | 다중선택 | `포인터`,`메모리` | tags (taxonomy allowed_tags 안에서만) |
| 난이도 | 선택 | `입문`/`기초`/`중급`/`고급` | level 매핑 |
| slug | 텍스트 | `pointer-cpp` | URL `/p/{slug}/`. 영문 소문자, **불변** |
| description | 텍스트 | 한 줄 요약 | 목록 카드 설명 |
| 상태 | 선택 | (아래 상태머신) | 진행/발행 추적 |
| 처리방식 | 선택 | `신규`/`기존글추가` | 중복 누적 정책 |
| 발행일 | 날짜 | `2026-06-06` | Actions가 발행 후 기록 |
| 발행커밋 | 텍스트 | `a1b2c3d` | Actions 기록 (발행 증거) |
| 발행URL | URL | `.../p/pointer-cpp/` | Actions 기록 (발행 증거) |
| 통과근거 | 텍스트 | "변형문제 2개 정답 + 직접 출력 제시" | **채점 통과 시 Claude가 기록**(무결성) |
| 오류요약 | 텍스트 | "tag 'xxx' taxonomy에 없음" | Actions 실패 시 기록 |

### 상태(Select) 값 — 상태머신

`공부중` → `발행준비` → `처리중` → `발행완료` / `발행실패`,  그리고 채점 미달 시 `재학습필요`.

- **공부중**: 정리·출제·답안·채점 진행 중.
- **발행준비**: 엄격 채점 통과 + 블로그 본문 작성 완료. Actions의 발행 대상.
- **처리중**: Actions가 잡은 락(중복발행 방지). 사람이 손대지 말 것.
- **발행완료**: 발행커밋·발행URL 기록됨.
- **발행실패**: 오류요약 기록 → 사용자가 Claude로 고친 뒤 다시 `발행준비`.
- **재학습필요**: 채점 미달. 약점 기록 → 다시 공부 후 재채점.

## 2. 각 행의 본문(페이지 내용) — 고정 섹션

행을 열면 본문에 항상 이 섹션들. **형식 고정**이라야 Claude가 일관되게 뽑는다.

```
## 학습 정리            (디버깅로그 원료 — 경험 데이터)
  - 막힌 질문 (무엇을 하다 어디서)   ← 제일 중요, 절대 비우지 말 것
  - 처음 생각한 것 (착각)
  - 실제로 해본 코드                 ← 없으면 아직 글감 아님
  - 실행 결과 (출력·에러)            ← 사용자가 직접 돌린 것. 비면 발행 불가
  - 예상과 달랐던 점
  - 정정된 이해
  - 나만의 규칙
  - 아직 모르는 것
  - 참고한 자료

## 복습 문제            (Claude가 출제 — 객관식 + 변형 서술형)

## 내 답안              (사용자가 작성)

## 채점 결과            (Claude — 통과/미달 + 통과근거 또는 약점·재학습 포인트)

## 블로그 본문          (채점 통과 시 Claude가 작성 — Actions가 그대로 추출)
  <!-- BLOG_MD_BEGIN -->
  ```markdown
  (SYSTEM-SPEC 4-1 (3) 구조의 완성 글. front matter는 넣지 않음 — Actions가 DB 속성에서 조립)
  ```
  <!-- BLOG_MD_END -->
```

> **왜 BLOG_MD_BEGIN/END 코드블록인가** — 노션 블록을 마크다운으로 "똑똑하게 변환"하면 코드펜스·표·콜아웃에서 깨진다. 대신 완성 마크다운을 코드블록에 raw로 넣고 Actions는 **변환 없이 그대로 추출**한다(SYSTEM-SPEC 5절 규칙 7).
> **front matter는 본문에 넣지 않는다** — 제목/slug/태그/날짜 등은 DB 속성에서 Actions가 조립(YAML 깨짐 방지, 규칙 6).

## 3. 매핑

**context → 카테고리(블로그 표시명)** — 허용 목록 정본 = `data/taxonomy.yaml`

| context | 카테고리 |
|---|---|
| cpp | C++ |
| unreal | Unreal |
| blender | Blender |
| tripo | Tripo 3D |

**난이도 → level**

| 노션 | level |
|---|---|
| 입문 / 기초 | beginner |
| 중급 | intermediate |
| 고급 | advanced |

## 4. 흐름 (사용자 입장)

1. **어디서나(태블릿)** Claude 앱으로 공부 → 노션 행 추가, `## 학습 정리` 채움.
2. Claude가 `## 복습 문제` 출제 → 내가 `## 내 답안` 작성.
3. Claude가 엄격 채점 → **통과**면 `## 블로그 본문` 작성 + 상태 `발행준비` + 통과근거 기록. **미달**이면 약점 적고 상태 `재학습필요`.
4. **하루 1번** GitHub Actions가 `발행준비` 행을 자동 발행 → 상태 `발행완료`.

> 채점·글작성은 전부 무료 Claude 앱. 발행만 GitHub 자동. 집 컴퓨터 불필요.
