# 학습 블로그 — 검증 게이트 + 자동발행 시스템 (정본 SYSTEM-SPEC)

> 이 문서가 시스템의 **단일 정본**이다. 구현·노션·미래의 나(클로드) 모두 이 문서를 기준으로 한다.
> 스택: **Hugo(커스텀 테마) + GitHub Pages**. 라이브: https://banchan723.github.io
> 사용자: 프로그래밍 초보, Windows + 태블릿/폰. 주제 = C++·언리얼·블렌더·Tripo 3D 등 계속 늘어남.
> 갱신: 2026-06-07 — **문서 통합.** 옛 NOTION-DB.md·GRADING-PROTOCOL.md·COWORK-PUBLISH.md·NEXT-SESSION.md를 이 한 파일로 흡수/삭제. 셋업 완료 + 자동발행 검증 통과(배포 연결 workflow_run 추가).

## 목차
- 0~3: 목표·아키텍처·결정·한계
- 4~4-2: front matter / 콘텐츠 원칙(v3) / **엄격 채점 규칙(게이트)**
- 5: 절대 규칙
- 6~6-2: 노션 매핑 / **DB 전체 스키마** / **행 본문 템플릿 + BLOG_MD 계약**
- 7~12: 상태머신·자동화부품·보안·디자인·셋업·NO-GO

> **운영 계약(요약 금지·원문 보존 대상):** 4-2 채점 규칙, 6-1 DB 스키마, 6-2 행 템플릿/BLOG_MD 마커. 앱·CI가 실제로 의존한다.

---

## 0. 한 줄 목표

**공부한 걸 진짜 이해했는지 검증되면, 태블릿만으로도 알아서 블로그에 글이 올라간다.**
사용자는 *공부 + 답안 작성*만 한다. 출제·채점·글작성은 무료 Claude 앱이, 발행은 GitHub이 한다. 집 컴퓨터 안 켜도 됨.

## 1. 아키텍처 — 3구역

```
[① 공부·검증]  어디서나 — 태블릿/폰, Claude 앱 + Notion 커넥터 (무료 구독)
     공부 대화 → 노션에 학습 정리(디버깅로그 원료)
        → 내가 복습 문제 출제(객관식 + 변형 서술형)
        → 사용자가 노션에 답안 작성
        → 내가 엄격 채점
             ├ 이해 완료 → 노션 본문에 '완성된 블로그 마크다운' 작성 + 상태=발행준비
             └ 이해 부족 → 약점·재학습 포인트 기록 + 상태=재학습필요 (다시 공부)
                 │
                 ▼
[② 발행]  GitHub Actions — 하루 1번 자동(+수동 버튼), 무인·기계적·무료 (LLM 없음)
     발행준비 행 폴링 → 본문 추출 + 메타로 front matter 조립 → 이미지 고정
        → 검증 스크립트 → content/post/{slug}/index.md 커밋 → push
        │  (push가 기존 hugo.yml을 트리거)
        ▼
[③ 빌드·배포]  hugo.yml — Hugo 빌드 → GitHub Pages 배포 → banchan723.github.io
```

- **머리 쓰는 일(출제·채점·글작성)** = 무료 Claude 앱. **GitHub은 노션→파일→커밋의 "멍청한 파이프"** 라 Claude API 키 불필요, 공짜.
- 노션이 "공부 장소(어디서나)"와 "발행 장소(GitHub)"를 잇는 다리.
- 집 데스크탑(이 Claude Code)은 **선택** — 이미지 많은 글, 디버깅, 큰 구조 변경 때만.

## 2. 확정된 핵심 결정 (2026-06-06 사용자 승인)

| 항목 | 결정 | 의미 |
|---|---|---|
| 발행 방식 | **B1 (채점은 대화, 발행만 자동)** | 글작성은 무료 Claude 앱이 그 세션에서. GitHub은 옮기기만. 비용 0 |
| 채점 강도 | **엄격 — 진짜 학습 강제** | 변형 문제 + 코드 주제는 **사용자가 직접 돌린 실행 결과/에러**를 붙여야 통과. 통과 근거를 노션에 기록 |
| 발행 승인 | **통과 = 바로 발행** | 별도 승인 단계 없음. 채점 통과되는 그 대화에서 초안을 봤으니 그게 검토 시점 |
| 중복 처리 | **기존 글에 누적** | 같은 개념 재학습은 새 글 아니라 기존 글에 `## 추가 학습` append. 중복키 = canonical_topic + context |
| 발행 주기 | **하루 1번 자동 + 수동 버튼** | 매일 정해진 시각 cron + 급할 때 workflow_dispatch |

## 3. 알고 가는 한계 (선택 아님, 수용)

- **자가채점 관대편향** — 같은 Claude가 가르치고 채점 → 후해질 수 있음. 엄격모드 + 변형문제 + 통과근거 기록으로 완화하나 완전 제거 불가. (옵션: 후에 Codex 교차채점 추가.)
- **코드 출력 환각** — Claude가 실행결과를 지어낼 수 있음 → 엄격모드가 "**네가 직접 돌린 출력**"을 요구하는 이유. 이게 환각 방어이자 "진짜 해봤다" 증거.
- **사용량 한도** — Claude 앱·노션 구독 한도 존재. 저빈도면 안 걸림.
- **습관 붕괴** — 며칠 밀리면 발행준비 큐만 쌓임. 시스템 문제 아님(원래 NO-GO 신호).
- **통과=바로발행** — 글 자체 오류(오타·사실오류)는 발행 후 다음 세션에 수정. 검증기는 형식만 보고 사실성은 못 봄.
- **발행완료 마킹 vs 실제 push** — notion_publish.py는 검증 통과 후 `발행완료`로 마킹하지만 git commit·push는 다음 워크플로 스텝이다. push가 실패하면(드묾·concurrency 직렬화로 완화) 행이 거짓 발행완료로 남을 수 있음. 정확한 마감(커밋 SHA 기록 포함)은 push 성공 후 finalize 단계로 옮기는 게 정석 — **TODO**. (검증 실패 케이스는 이미 발행완료 전 차단됨.)

## 4. 글 front matter 스키마 (모든 글 공통, 절대 규칙)

`content/post/<slug>/index.md` 상단. **이 front matter는 Claude가 직접 쓰지 않는다 — GitHub Actions가 노션 DB 속성에서 조립한다**(YAML 파싱 사고 방지).

```yaml
---
title: "한글 제목 가능"          # 항상 따옴표 (콜론 사고 방지)
date: 2026-06-06
slug: "pointer-cpp"             # 영문 소문자. 한번 정하면 불변. URL = /p/{slug}/
description: "목록에 보일 한 줄 요약"
categories:                    # context에서 자동 매핑 (cpp→C++ 등)
  - "C++"
tags:                          # 해당 category 의 allowed_tags 안에서만
  - "포인터"
canonical_topic: "pointer"     # 개념 식별자(영문 소문자) — 중복판정 키①
context: "cpp"                 # 맥락 — 중복판정 키②
level: "beginner"             # beginner / intermediate / advanced
---
```

**중복 판정 키 = `canonical_topic` + `context`.** 같은 키 → 같은 글(누적), 다른 키 → 새 글.
예: `pointer`+`cpp` ≠ `pointer`+`unreal`.

## 4-1. 글 작성 원칙 (콘텐츠 정본) — 2026-06-06 개정 (v3: 매체 일반화)

> **핵심 철학: 설명은 하되, 설명의 근거를 반드시 내 것에서 가져온다.**
> 금지 대상은 "설명"이 아니라 **"근거 없는 설명"** 이다. 설명을 막으면 독자가 못 따라오고, 설명을 풀어버리면 AI 일반론(슬롭)이 된다.
> **본문은 초보 독자가 따라오는 순서로 쓰고, 증거는 주장 옆에 배치한다.**
>
> **v3 일반화(2026-06-06 사용자 승인):** 글을 매체별로 나누지 않는다. **골격은 하나**, 그 안의 "확인한 증거" 섹션이 매체에 맞는 증거를 담는다. 매체(modality)는 노션 필드 하나로 두고, **CI는 그 값에 따라 어떤 증거를 필수로 볼지만 바꾼다.** 영상은 *출처일 뿐 근거가 아니다* — 근거는 항상 "내가 재현·변형한 것".
>
> **칼 한 문장 (모든 설명 문단에 적용):**
> *"이 문단은 내가 실제로 막혔던 문제 · 직접 돌린 코드/출력 · 직접 찍은 스크린샷(+재현단계) · 영상 보고 내가 재현·변형한 것 · 채점 통과 답변 중 하나에 기대고 있는가?"*
> 아니면 삭제하거나 `## 아직 모르는 것`으로 내린다.

### (0) 매체(modality) — 증거의 형태를 정한다
노션 `매체` 필드값. 글 골격은 같고, "확인한 증거" 섹션에 무엇이 들어가야 하는지만 다르다.

| 매체 | 대상 | 필수 증거 |
|---|---|---|
| `code` | C++ 등 코드 실행 | 직접 친 코드 + **직접 돌린 출력/에러** |
| `visual` | 블렌더·언리얼 에디터 | 스크린샷 + **재현 단계 + 설정값**(메뉴 경로·노드명·수치). 스크린샷 단독 불인정 |
| `video` | 유튜브 강의 등 | 출처(URL+제목+타임스탬프) + **내가 재현하거나 변형한 결과물** |

한 글에 여러 매체가 섞일 수 있다(예: 영상 보고 언리얼에서 따라함 = video+visual). 그때는 해당 증거를 모두 충족.

### (1) 설명의 출처 강제
- **허용 근거**: ① 채점에서 사용자가 자기 말로 통과한 설명 ② 직접 작성·실행한 코드 + 출력 ③ 직접 찍은 스크린샷 + 재현 단계·설정값 ④ 영상에서 배운 걸 **내가 재현·변형한 결과** ⑤ 틀렸던 가설과 정정된 이해 ⑥ 공식 문서/교재의 최소 정의.
- **금지**: 증거와 연결 안 된 일반 설명, "포인터는 ~이다" 교과서 문장만, "쉽게 말해/중요한 건/일반적으로/효율적입니다"로 시작하지만 증거 없는 문단, AI가 지어낸 비유·예시, **영상 내용을 그대로 베낀 요약**(출처는 근거가 아님).
- 글 작성 시스템(Claude 앱)은 각 설명 문단의 근거를 자각하고 쓴다. 근거 없으면 안 쓴다.

### (2) 제목 규칙
- **제목 = 개념명 그 자체.** 예: "C++ 포인터", "Unreal 액터". 한글 가능, 항상 따옴표.
- 감성 비유 제목·"완벽 정리"·"쉽게 이해하기" 류 과장 제목 금지.
- **개념 1개 = 살아있는 학습 문서 1개.** 같은 개념 재학습은 기존 글에 누적(5절).

### (3) 본문 골격 (매체 무관 공통) — 필수 섹션
초보가 처음 봐도 따라오는 순서. 모든 매체 동일.
1. **`## 이 글에서 이해할 것`** — 한 줄 목표. 독자가 이 글로 뭘 얻는지.
2. **`## 읽기 전 최소 배경`** — "이 정도는 안다고 가정"(길게 설명 금지). **비우지 말 것.**
3. **`## 내가 막혔던 점 / 틀린 가설`** — 실제 질문·착각. **선택(2026-06-07 개정)**: 있으면 가장 강한 사람 흔적이라 권장. **없으면(이미 아는 개념) 생략 가능** — 단 그 경우 4·6의 강한 증거(직접 돌린 출력/재현 + 변형문제 통과 + 자기말 설명)가 반드시 있어야 발행. 막힘이 없는데 **지어내는 것은 금지**(가짜 서사 = 슬롭).
4. **`## 확인한 증거`** — 매체(0절)에 맞는 증거. 주장마다 증거를 붙인다. 증거 종류별 규칙:
   - **code**: 직접 친 최소 코드 + **직접 돌린 출력/에러**. 비면 발행 금지. 환경 의존값(주소 등)은 지어내지 말고 관계 중심으로.
   - **visual**: 스크린샷(캡션에 "무엇을 확인했는지") + **재현 단계 + 설정값**(메뉴 경로·노드명·수치). 스크린샷만 있으면 발행 금지.
   - **video**: 출처(URL·제목·타임스탬프) + **내가 재현/변형한 결과물**(코드·스크린샷). 영상 요약만 있으면 발행 금지.
5. **`## 그래서 이렇게 이해했다`** — 채점 통과 답변 기반의 정정된 이해 + 내 규칙. 틀린 이해에서 바뀐 지점 명시.
6. **`## 다른 예시에 적용해보기`** — 배운 걸 살짝 다른 상황에 적용한 짧은 예 1개. 초보 친화 + 베끼기 방지(이해 못 하면 못 씀).
7. **`## 아직 모르는 것`** — 다음에 더 팔 거리 + 다음 글 연결. 신뢰 장치.
8. **`## 확인 질문`** — 독자 점검용 1개 이상. **사용자가 통과한 변형문제의 축약판**(AI·영상 그대로 아님).
- **추가 학습 시**: `## 추가 학습 (YYYY-MM-DD)` 아래 4~6 블록 반복. 5회 넘으면 시리즈 분리.

### (4) 정확성·증거 무결성 방어
- 단정 일반론 줄이기(범위 한정: "이번에 확인한 건 ~까지"). 모르는 것은 `## 아직 모르는 것`에. 노션에 없는 지식 AI 임의 추가 금지. 면책 한 줄은 글 끝 작은 글씨로.
- **스크린샷 조작/오해 방어**: 스크린샷은 "결과물"일 뿐, 단독 증거로 쓰지 않는다. 항상 재현 단계·설정값과 함께. 가능하면 전/후 비교. (완전 차단은 불가 — 재현정보 동반으로 신뢰성 확보.)
- **영상 베끼기 방어**: 영상은 출처. 통과 기준은 "영상을 보고 내가 재현·변형한 것"이 있는가. 확인 질문도 영상 그대로가 아니라 변형 문제.

### (5) CI readability 프록시 (검증기가 강제 — 형식·추적성만, 진짜 이해도는 사람이 봄)
- 필수 섹션(위 1·2·4·5·7·8) 존재 여부. (3 "막혔던 점", 6 "다른 예시"는 경고 수준 — 3은 선택, 위 (3) 참고.)
- **매체별 증거 강제**(노션 `매체` 값 기준):
  - `code`: 코드 블록 있는데 출력(`## 확인한 증거` 내 결과)이 비면 실패.
  - `visual`: 이미지 파일 참조 존재 + 재현 단계 텍스트 존재. 둘 중 하나 없으면 실패.
  - `video`: 출처 URL 존재 + 재현/변형 결과(코드 또는 이미지) 존재. 없으면 실패.
- 이미지마다 캡션/alt 텍스트 필수.
- 상투어("쉽게 말해/일반적으로/중요합니다/효율적입니다") 과다 → 경고.
- 한 문장 120자·한 문단 5문장 초과 → 경고.

## 4-2. 엄격 채점 규칙 (게이트 — Claude 앱이 따름)

> **운영 계약(원문 보존).** 옛 GRADING-PROTOCOL.md 흡수. 자기 블로그라 강제력이 약하므로 게이트의 신뢰는 이 규칙 + 자기규율에 달림.

### 통과 기준 ("맞췄나"가 아니라 "다른 상황에 적용되나") — 모두 만족해야 통과
1. **자기 말로 설명** — 개념을 사용자 자신의 언어로(교과서·AI 문장 복붙 아님).
2. **변형 적용** — 방금 본 예제와 **다른 입력/조건**의 변형 문제를 풀어낸다(암기 배제).
3. **직접 돌린 출력** — 코드 주제(code)면 **실제로 실행한 출력/에러**를 제시. 시각 주제(visual)는 결과 스크린샷+재현단계, 영상(video)은 재현·변형 결과물로 대체.
4. **착각 정정 (해당 시)** — 막힌 게 있었으면 처음 틀린 생각과 그게 어떻게 바뀌었는지 말한다. **이미 아는 개념이면 생략 가능** — 대신 1~3(자기말·변형·직접 출력)이 더 확실해야 한다. 막힘을 **지어내지 말 것**.

> **막힌 점 없는 글(정리형)의 통과선**: 1·2·3을 모두, 그리고 "포인터는 주소다" 수준의 단순 진술이 아니라 **개념을 조작한 흔적**(값 변경·역참조·실행 전 예측 등)이 자기 말 설명에 있어야 한다. 증거 흉내(AI스러운 출력·일반론만)는 불통과.

→ 통과 시 노션 `통과근거`에 한 줄 기록(예: "변형문제 2개 정답 + 직접 출력 제시 + 8바이트 의문 정정"). 자가채점 관대편향을 막는 흔적.

### 채점 절차
1. `## 학습 정리`를 읽고 **복습 문제 출제**: 객관식 1~2(개념) + **변형 서술형 1~2**(적용). 노션 `## 복습 문제`에 기록.
2. 사용자가 `## 내 답안` 작성 → 채점.
3. **통과**: `## 채점 결과`에 통과 + 통과근거 → `## 블로그 본문` 작성(4-1 (3) 구조) → 상태 `발행준비`.
4. **미달**: `## 채점 결과`에 **어디서 틀렸는지 + 뭘 다시 볼지** → 상태 `재학습필요`. 막힌 지점을 좁혀 재출제.

### 채점 무결성 (스스로 경계)
- **관대편향 주의**: 같은 Claude가 가르치고 채점 → 후해지기 쉽다. 애매하면 통과시키지 말 것.
- **답안 베끼기 방지**: 마지막에 짧은 구두형 질문("X를 Y로 바꾸면 왜 깨지나?", "A와 B 차이는?")으로 진짜 이해 확인.
- **환각 방어**: 코드 출력은 **사용자가 직접 돌린 것만**. Claude가 출력을 지어내지 않는다.
- **"그냥 통과시켜줘" 거절**: 게이트 무력화임을 알리고, 통과근거 없는 통과는 거절(블로그 신뢰가 무너짐).

## 5. 절대 규칙

1. **slug 불변** — 최초 발행 후 변경 금지(주소 깨짐). 영문 소문자.
2. **categories·tags 는 `data/taxonomy.yaml` 에 있는 것만.** 새로 필요하면 사용자 승인 후 추가.
3. **context 는 category 에 묶인 값과 일치.** (검증기가 강제.)
4. **발행 전 중복 판정** — `canonical_topic`+`context`로 기존 발행글(노션 상태=발행완료) 검색 → 있으면 처리방식=기존글추가.
5. **"기존글추가"는 같은 slug 글 하단에 `## 추가 학습 (날짜)` append**, 원본 보존. 5회 넘으면 시리즈 분리.
6. **front matter는 Actions가 노션 속성에서 조립.** Claude는 본문 마크다운만 작성.
7. **본문은 노션 `BLOG_MD_BEGIN`/`BLOG_MD_END` 마커 사이에 평범한 마크다운으로**(코드블록 래퍼 금지 — 6-2 참고). Actions가 노션 블록을 마크다운으로 변환 추출.
8. **발행 성공 = 커밋 + hugo.yml 빌드 성공 + 실제 URL 접속.** 셋 다 돼야 상태=발행완료.
9. **검증 실패 시 본문 자동 수정 금지.** 노션에 오류만 적고 멈춤 → 사용자가 Claude로 고침.
10. **비밀정보·유료자료 스캔** — 채점/발행 전.

## 6. 노션 → front matter 매핑

전체 칼럼은 6-1 절. 요약:

| 노션 DB 속성 | front matter |
|---|---|
| 제목 | title (Actions가 따옴표 처리) |
| slug | slug |
| canonical_topic | canonical_topic |
| context (select) | context + categories(자동 매핑) |
| 매체 (multi-select) | modality (code·visual·video — 검증기 분기, 테마 표시용) |
| 태그 (multi-select) | tags |
| 난이도 (select) | level (입문·기초→beginner, 중급→intermediate, 고급→advanced) |
| 영상출처 (text) | (본문용 — front matter엔 안 넣고 본문 "확인한 증거"에 들어감) |
| description | description |
| 발행일 | date |
| (페이지 본문 코드블록) | 글 본문 전체 |

## 6-1. 노션 "학습 기록" DB 전체 스키마 (운영 계약)

> 옛 NOTION-DB.md 흡수. DB = `https://app.notion.com/p/77557a111e934f4cbfb363b0e9746894` (NOTION_DB_ID=`77557a111e934f4cbfb363b0e9746894`, data source `3041c1b9-b0b3-4104-8e4c-ee6e3d8a703c`). 부모 페이지 "학습 블로그". 각 **행 = 학습 주제 1개**.

| 칼럼 | 타입 | 값 예시 | 용도 |
|---|---|---|---|
| 제목 | 제목(Title) | `C++ 포인터` | 글 제목 (한글 OK, Actions가 따옴표 처리) |
| canonical_topic | 텍스트 | `pointer` | **중복키①** (영문 소문자) |
| context | 선택 | `cpp`/`unreal`/`blender`/`tripo` | **중복키②** + 카테고리 매핑 |
| 매체 | 다중선택 | `code`/`visual`/`video` | **증거 형태** — 검증기가 매체별 필수 증거 강제 (4-1 (0)) |
| 영상출처 | 텍스트 | `제목 \| URL \| 12:30` | video 매체일 때 출처. 본문 "확인한 증거"에 들어감 |
| 태그 | 다중선택 | `포인터`,`메모리` | tags (taxonomy allowed_tags 안에서만) |
| 난이도 | 선택 | `입문`/`기초`/`중급`/`고급` | level 매핑 (입문·기초→beginner, 중급→intermediate, 고급→advanced) |
| slug | 텍스트 | `pointer-cpp` | URL `/p/{slug}/`. 영문 소문자, **불변** |
| description | 텍스트 | 한 줄 요약 | 목록 카드 설명 |
| 상태 | 선택 | (7절 상태머신) | 진행/발행 추적 |
| 처리방식 | 선택 | `신규`/`기존글추가` | 중복 누적 정책 |
| 발행일 | 날짜 | `2026-06-06` | Actions가 발행 후 기록 |
| 발행커밋 | 텍스트 | `a1b2c3d` | Actions 기록 (발행 증거) |
| 발행URL | URL | `.../p/pointer-cpp/` | Actions 기록 (발행 증거) |
| 통과근거 | 텍스트 | "변형문제 2개 정답 + 직접 출력 제시" | **채점 통과 시 Claude 기록**(무결성) |
| 오류요약 | 텍스트 | "tag 'xxx' taxonomy에 없음" | Actions 실패 시 기록 |

## 6-2. 행 본문 템플릿 + BLOG_MD 계약 (운영 계약)

> 옛 NOTION-DB.md 흡수. 행을 열면 본문에 항상 이 섹션들. **형식 고정**이라야 Claude가 일관되게 뽑고 Actions가 정확히 추출한다.

```
## 학습 정리            (디버깅로그 원료 — 경험 데이터)
  - 막힌 질문 (무엇을 하다 어디서)   ← 제일 중요, 절대 비우지 말 것
  - 처음 생각한 것 (착각)
  - 확인한 증거 (매체에 맞게 — 아래 중 해당하는 것)
      · code  : 실제로 해본 코드 + 직접 돌린 실행 결과(출력·에러)
      · visual: 스크린샷 + 재현 단계 + 설정값(메뉴 경로·노드명·수치)
      · video : 출처(제목·URL·타임스탬프) + 내가 재현/변형한 결과물
    ← 매체별 필수 증거 없으면 발행 불가 (스크린샷 단독·영상 요약만 = 불가)
  - 예상과 달랐던 점
  - 정정된 이해
  - 나만의 규칙
  - 다른 예시에 적용해본 것
  - 아직 모르는 것
  - 참고한 자료

## 복습 문제            (Claude가 출제 — 객관식 + 변형 서술형)

## 내 답안              (사용자가 작성)

## 채점 결과            (Claude — 통과/미달 + 통과근거 또는 약점·재학습 포인트)

## 블로그 본문          (채점 통과 시 Claude가 작성)
  <!-- BLOG_MD_BEGIN -->
  (여기에 4-1 (3) 구조의 완성 글을 **평범한 마크다운으로** 쓴다.
   ## 헤딩, 본문 문단, 코드 예시는 ```cpp 코드블록 — 노션이 각각 네이티브 블록으로 저장.
   H1 제목·front matter는 넣지 않음. ## 이 글에서 이해할 것 부터 시작.)
  <!-- BLOG_MD_END -->
```

> **⚠️ 본문을 코드블록으로 감싸지 말 것 (2026-06-07 개정).** 옛 방식(전체를 ```` ```markdown ```` 한 블록에 raw로)은 본문 안의 ``` 코드펜스와 충돌한다 — 노션 마크다운 import가 4백틱조차 3백틱으로 정규화해 블록이 쪼개진다(실측 확인). 그래서 **본문은 마커 사이에 평범한 마크다운으로** 쓰고, `notion_publish.py`가 **노션 블록 구조를 마크다운으로 변환**한다(heading→`##`, code→```펜스, paragraph→텍스트, 인라인 코드·볼드·링크 보존).
> **지원 블록**: 헤딩(1~3)·문단·코드·불릿/번호 리스트·인용·구분선·이미지. 표·콜아웃·Hugo shortcode는 깨질 수 있으니 본문에 쓰지 말 것.
> **front matter는 본문에 넣지 않는다** — 제목/slug/태그/날짜는 DB 속성에서 Actions가 조립(규칙 6).

## 7. 상태머신 (노션 `상태` select)

```
공부중 ──(채점 통과 + 본문 작성)──▶ 발행준비 ──(Actions 잡음)──▶ 처리중
   ▲                                                              │
   └──(이해 부족)── 재학습필요 ◀────┐                    ┌────────┴────────┐
                                    │                    ▼                 ▼
                                 (다시 공부)          발행완료          발행실패
                                                   (커밋·URL 기록)   (오류요약 기록 → Claude로 고침)
```

- **공부중**: 정리·출제·답안·채점 진행 중.
- **발행준비**: 채점 통과 + 본문 마크다운 작성 완료. Actions의 발행 대상.
- **처리중**: Actions가 잡은 락(중복발행 방지). 사람이 손대지 말 것.
- **발행완료**: 발행커밋·발행URL 기록됨.
- **발행실패**: 오류요약 기록. 사용자가 Claude로 고친 뒤 다시 발행준비로.
- **재학습필요**: 채점 미달. 약점 기록. 다시 공부 후 재채점.

## 8. 자동화 부품

| 파일 | 역할 |
|---|---|
| `scripts/notion_publish.py` | 노션 폴링 → 본문 추출 → 이미지 고정 → front matter 조립 → 파일 작성 → 상태 마감. CI에서 실행 |
| `.github/workflows/publish-from-notion.yml` | 하루 1번 cron + 수동. 위 스크립트 실행 → 검증 → 커밋·push (contents:write) |
| `scripts/validate_posts.py` | front matter·taxonomy·중복 검증. 발행 전 차단 (기존) |
| `.github/workflows/hugo.yml` | push **또는 publish 워크플로 완료(workflow_run)** 시 빌드·배포. GITHUB_TOKEN 푸시가 push 트리거를 못 하므로 workflow_run으로 연결 |
| 본 문서 4-2절 (엄격 채점 규칙) | Claude 앱이 따르는 채점 게이트 |
| 노션 "학습 기록" DB | 학습·검증·발행 상태의 단일 수신함 (6-1 스키마) |
| 노션 "블로그 발행 규칙" 페이지 | taxonomy·스키마·템플릿 (Claude 앱이 매번 읽음). 정본=본 문서 |
| 노션 "학습 로드맵 & 진도" 페이지 | 진도 추적 (Claude 앱이 세션마다 읽고 갱신) |

## 9. 보안

- **노션 토큰**은 해당 DB만 공유한 Internal Integration Token. GitHub **Secret**(`NOTION_TOKEN`)으로만. 로그·커밋에 노출 금지.
- **Actions 권한 최소화**: publish 워크플로는 `contents: write`만.
- **본문 위험 콘텐츠 차단**: 발행 전 위험 HTML/스크립트/iframe 스캔.
- **이미지**: 외부 URL 직접삽입 금지. 노션 임시 URL은 Actions가 받아 repo에 고정.

## 10. 디자인 (사용자 확정) — 변경 없음

커스텀 테마, 다크 기본, 왼쪽 사이드바(소개/아카이브/검색 + 다크 토글), 카드형 글 목록(카테고리 칩·태그·읽는시간), 코드블록 줄번호 거터. 레퍼런스 = minyeamer 룩.

## 11. 사용자만 할 수 있는 일 (1회 셋업)

- 노션 **Internal Integration** 발급 + "학습 기록" DB를 그 통합에 **공유**.
- GitHub repo **Secrets** 등록: `NOTION_TOKEN`, `NOTION_DB_ID`.
- GitHub Settings → Pages → Source = "GitHub Actions" (이미 됨).
- Settings → Actions → Workflow permissions = "Read and write" (또는 워크플로 `permissions`로 지정 — 본 설계는 후자).
- 모바일/태블릿 Claude 앱에 **Notion 커넥터 연결**.

## 12. NO-GO 신호

- 한 달 돌려보고 "노션 정리·답안이 공부보다 부담" → 게이트 완화 또는 시스템 축소.
- 채점을 자꾸 우회(그냥 통과)하게 됨 → 게이트 무의미. 강도 재조정.
- 한 달에 글 1개도 안 쌓임 → 자동화 문제 아님, 공부 습관부터.
