# ============================================================
#  learning.yaml — 사이드바 '요즘 공부 중' 위젯 데이터 (단일 소스)
#
#  채우는 방법(둘 중 하나):
#   1) 노션 학습 진행 DB에서 발행 시 동기화 (목표 — DB 갖춰지면 자동)
#   2) "요즘 공부 중 갱신해줘"로 클로드가 직접 수정 → 다음 발행 때 반영
#
#  pct = 체감 이해도(주관 수치, 객관 측정값 아님):
#        막 시작 ~20 / 어느 정도 ~50 / 거의 ~80 / 마무리 ~95
#  color = 진행바 색. 카테고리 색을 따라가면 보기 좋다:
#          C++=var(--blue) · Unreal=var(--cyan) · Blender=var(--orange) · Tripo=var(--purple)
#          그 외: var(--green)/var(--amber)/var(--pink) 자유.
#
#  항목이 0개면 위젯은 자동으로 숨겨진다.
# ============================================================

items: []
#  예시 (실제 공부 중인 걸로 교체):
#  - label: "C++ 포인터"
#    pct: 50
#    color: "var(--blue)"
