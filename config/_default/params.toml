# 커스텀 테마 파라미터 (BanChan / blog)
mainSections   = ["post"]
rssFullContent = true

# ── 프로필 (사이드바·헤더·푸터에서 한 곳으로 노출) ──────────
[profile]
  name     = "BanChan"
  handle   = "@banchan"
  role     = "self-taught everything"
  bio      = "퇴근 후 2시간, 독학으로 배우는 모든 것."
  location = "Seoul, KR"
  since    = "2023"
  avatar   = "BC"          # 아바타 이니셜 (이미지 대신 텍스트)

# ── '요즘 공부 중' 위젯 데이터는 data/learning.yaml 로 분리 ──
#    (노션 학습 진행 DB → 발행 시 동기화의 목적지. 클로드가 갱신)

# ── 푸터 (연도 등 보존) ──────────────────────────────────
[footer]
  since      = 2026
  customText = "혼자 공부한 기록"
