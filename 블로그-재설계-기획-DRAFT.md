# 블로그 재설계 기획 (v2 — 방향 확정됨 ✅)

> **상태: 기획 방향 확정 (2026-06-09 사용자 승인).** 아래 1~6절 방향은 GO. 정본(SYSTEM-SPEC.md)은 아직 안 고침 — 설계도 확정 후 개정.
> 작성: 2026-06-09. 사용자와의 발견 대화에서 도출. v2 = 사용자의 "범위" 교정 반영.

---

## ⏩ 다음 세션 시작점 (HANDOFF — 여기부터 읽어라)

**기획 방향은 확정됐다(아래 GO). 다음 할 일 = 설계도(어떻게 구현할지) 작성 → 구현.**

확정된 핵심 (사용자 승인):
- 레버 = **글쓰기가 아니라 학습 프로토콜.** 토픽을 *완전·깊게* 가르치고 *전체 이해*를 검증한 뒤에야 발행. 그럼 글이 자동으로 깊어짐.
- 글은 **내 공부 범위를 절대 안 넘음** (공부 블로그 정체성·증거강제 유지). AI가 범위 밖을 채우지 않음.
- 발행 전 **자동 교차검증**으로 사실오류·틀린 이해 차단.
- 글 = **교육 레퍼런스** 스타일 (깊고 체계적, "언리얼 공식문서 느낌"). 한 글 = 한 토픽, 곁가지 제거.
- **토픽 경계 = roadmap.yaml 트리 노드.** 공부도 글도 노드 단위. 충분히 크면 하위 노드로 분리.

다음 세션이 할 일 (순서):
1. **이 문서 + SYSTEM-SPEC.md 다시 읽기.** (정본 4-1 콘텐츠철학, 4-2 채점, validate_posts.py가 바뀔 대상)
2. **설계도 작성** — 7절 미결을 해소:
   - ★ 최대 미결: **교차검증을 어디서 돌리나** (Claude앱 작성단계 / CI LLM API / 집 Claude Code). 비용·"공짜 파이프" 원칙과 트레이드오프.
   - 학습 프로토콜 강화 문구를 어디에 박나 (SYSTEM-SPEC 4-2 + 노션 "블로그 발행 규칙" 페이지).
   - 새 글 구조 최종 확정 (4절 골격) + validate_posts.py 재설계.
   - 계층 네비 구현 방법 (roadmap.yaml → 인덱스 → hugo partial. Codex 레시피는 7절 참고).
   - 기존 6개 글 소급 재작성 여부.
3. **설계도 = 큰 설계 결정이므로 Codex 교차검증 필수** (CLAUDE.md 규칙).
4. 설계 확정 → SYSTEM-SPEC.md 개정 → 구현.

⚠ **절대 불변**: slug·URL(`/p/{slug}/`) 불변, roadmap URL 불변. 발행 파이프는 노션 "발행준비" 행만 처리.

---

## 0. 이 문서가 나온 과정 (= 추측이 아니라 사용자가 직접 고른 것)

블로그 글 품질이 "초딩 수준"이라는 불만에서 시작. "내가 바라는 이상형을 찾자"는 요청에 따라,
추상적 선택지 대신 **구체적 샘플에 반응하는 방식**으로 끌어냈다.

| 발견 | 사용자 의견 |
|---|---|
| 이 블로그는 누구를 위한 것? | **교육 자료 + 포트폴리오 증명 + 복습 노트 — 셋 다** |
| 글 스타일 (const 3종 비교) | **교육 레퍼런스** (개념·문법을 깊고 체계적으로. "언리얼 공식문서 느낌") |
| 정확성·신뢰 모델 | **발행 전 자동 교차검증 통과해야 발행** |
| ★ 글의 범위 (v2 교정) | **글은 내 공부 범위를 절대 안 넘는다.** 깊이는 'AI가 채워서'가 아니라 **'공부를 깊게 해서' 나온다.** |

---

## 1. 진단 — 글이 얕은 진짜 원인 (사용자 교정의 핵심)

**문제는 글쓰기가 아니라 공부(튜터링)다.**

지금 흐름: 포인터를 공부 → Claude가 "포인터가 뭔지"만 가볍게 알려줌 → 내가 "이해했다" → 발행.
→ **공부가 얕으니 글도 얕다.** 글 작성기를 아무리 바꿔도, 입력(내 이해)이 얕으면 출력도 얕다.

따라서 고칠 레버는 **학습 프로토콜**이다:
> 포인터면 포인터의 **모든 것**을 깊게 가르치고, 내가 그 **전체를 진짜 이해**했는지 검증한 뒤에야 글이 된다.
> 그러면 글은 **자동으로** 깊어진다 — 내가 깊게 공부했으니까.

**바뀌지 않는 것 (정체성 유지):**
- 글은 **내가 공부한 범위를 절대 넘지 않는다.** AI가 안 배운 걸 채우지 않는다. (= 공부 블로그)
- 글은 여전히 **내가 이해하고 직접 해본 것**이다 (증거강제·"내 것" 유지).

---

## 2. 한 줄 목표 (재정의)

**기존:** 공부한 걸 *이해했는지 검증되면* 자동 발행. (얕게 이해해도 통과)

**신규(제안):** 한 토픽을 *완전하고 깊게 이해*했을 때, 그 이해가 그대로 *교육 레퍼런스급 글*이 되어 자동 검증·발행된다.

핵심 = **"이해 완료"의 기준을 높인다.** "대충 알겠다" → "이 토픽 전체를 깊이 안다."

---

## 3. 핵심 전환 (★ SYSTEM-SPEC 정본을 바꾸는 결정 — 사용자 승인 필수)

| 항목 | 기존 (현 SYSTEM-SPEC) | 신규 (제안) |
|---|---|---|
| 무엇을 고치나 | (글 형식 위주) | **학습 프로토콜** — 토픽을 완전히 가르치고 완전히 검증 |
| "이해 완료" 기준 | 기본 개념 이해 + 변형문제 1~2 | **토픽 전체 깊이 이해** — 핵심 개념을 빠짐없이, 각각 직접 해봄 |
| 글 깊이 | 얕음 (얕게 공부해서) | 깊음 (깊게 공부해서) |
| 글 범위 | 내 공부 범위 | **내 공부 범위 (불변)** — 단 그 범위 자체가 깊어짐 |
| 슬롭(AI 일반론) 방어 | "내가 직접 한 것만 써라" | **유지** + 발행 전 자동 교차검증(사실오류 차단) 추가 |
| 글 구조 | 8개 고정 섹션 | 교육 레퍼런스 구조 (4절) |
| 곁가지(읽기 전 최소 배경 등) | 필수 강제 | **제거** (한 글 = 한 토픽 집중) |

> **무결성 모델:** 증거강제(내가 직접 해봄)는 그대로 두고, 그 위에 **교차검증**을 얹는다.
> 교차검증의 역할 = "AI가 범위 넘게 채웠나" 감시가 아니라, **내가 이해한 내용이 사실로 맞나**(틀린 이해·잘못된 설명) 걸러내는 것.

---

## 4. 새 글 구조 (교육 레퍼런스 템플릿 — 제안, 확정 시 조정)

**원칙: 한 글 = 한 토픽, 깊게. const 글이면 const만. 곁가지 금지.**
**모든 내용은 내가 공부·이해·직접 해본 것.** (범위 초과 금지)

제안 골격:
1. **개념 한 줄 정의** — 이게 뭔지 한 문장.
2. **왜 필요한가 / 언제 쓰나** — 동기.
3. **문법과 동작 — 내가 공부한 만큼 빠짐없이.** (const면 변수/멤버함수/참조/포인터 등 내가 다룬 케이스 전부)
4. **직접 돌린 예제** — 내가 친 코드 + 직접 돌린 출력. (증명·복습 기능이 여기 녹아듦)
5. **흔한 실수 / 주의점** — 내가 걸렸거나 확인한 함정.
6. **관련 토픽** — 상위/하위/형제 개념으로 링크 (6절 계층 네비가 자동 생성).

> 깊이는 4절 골격이 아니라 **3·5절을 내가 깊게 공부했기 때문에** 채워진다.
> 증명(포트폴리오)·복습은 별도 섹션이 아니라 본문에 통합: 내 예제 = 증거, 완전한 레퍼런스 = 복습 노트.

---

## 5. 시스템 흐름 (자동화 유지, 게이트 강화)

```
공부 (노션/Claude 대화)
   → Claude(튜터)가 토픽 '전체'를 깊게 가르침 + 내가 각 부분을 직접 해봄
   → ★ 강화된 이해 검증: 토픽 전체를 깊이 이해했는지 (얕으면 통과 안 됨)
   → Claude가 그 이해를 교육 레퍼런스 글로 작성 (내 범위 내)
   → ★ 자동 교차검증: 다른 모델(Codex 등)이 사실오류·틀린 이해 검사
        ├ 통과 → 발행 (content/post/{slug}/index.md 커밋 → hugo 빌드)
        └ 실패 → 오류 기록 → 수정/재학습 후 재검증
```

- **바뀌는 핵심 2개:** ① 학습 게이트가 "기본 이해" → "토픽 전체 깊이 이해"로 강화. ② 발행 전 사실 교차검증 추가.

---

## 6. 구조·네비게이션 (언리얼 공식문서 스타일 — 글 방향과 독립적으로 진행 가능)

- **Home / 로드맵 = 지식 트리 허브.** 한눈에 어디까지 왔고 어디로 갈지 (이미 /roadmap/ 있음, 허브로 강화).
- **글 내부 계층 네비.** roadmap.yaml(트리)을 단일 소스로:
  - 상단 breadcrumb: `C++ > 메모리 > 포인터`
  - 하단 자동 링크: 하위 토픽(포인터 → 스마트포인터·new/delete), 형제, 다음 추천 글
  - 구현: roadmap.yaml → 인덱스 생성 → hugo partial이 글의 topic_id 기준으로 자동 렌더.

---

## 7. 아직 정해야 할 것 (확정 후 다음 라운드 — 설계도 단계)

1. ~~"한 토픽을 다 공부했다"의 경계는?~~ **→ 확정 (2026-06-09): 토픽 경계 = roadmap.yaml 트리 노드.**
   공부도 글도 노드 단위. 함수포인터처럼 충분히 크면 트리에 하위 노드로 분리. 트리 = 토픽 경계의 단일 기준 + 계층 네비와 자동 연동.
2. **학습/튜터링 프로토콜 강화를 어디에 박나?** — SYSTEM-SPEC 4-2(채점) + 노션 "블로그 발행 규칙" 페이지(Claude 앱이 읽는 지침). 여기를 "토픽 완전 교육 + 완전 검증"으로 다시 씀.
3. **교차검증을 어디서 돌리나?** — 기존 "LLM 없는 공짜 파이프"와 충돌. 후보: (a) Claude 앱 작성 단계 자가+Codex 검증 / (b) CI가 LLM API(키·비용) / (c) 집 Claude Code 세션 검증.
4. **기존 6개 글** 소급 재작성/심화할지.
5. **validate_posts.py 재설계** — 8섹션 강제 → 교육 레퍼런스 구조 검증으로.
6. **깊이·난이도 표기** — 초보 친화 유지선.

---

## 8. 다음 행동

사용자가 이 v2를 읽고 **"맞다 / 여기는 다르다"** 교정 → 확정 시:
① SYSTEM-SPEC.md 정본 개정 ② 설계도(7절 미결 해소 + 구현 계획) 작성 — 이 단계에서 Codex 교차검증 ③ 구현.
